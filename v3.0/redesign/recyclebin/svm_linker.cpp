/*************************************************************************
    > File Name: svm_linker.cpp
    > Author: Li Jiaying
    > Mail: lijiaying1989@gmail.com 
    > Created Time: 2015年10月28日 星期三 17时08分44秒
 ************************************************************************/
#include <cstdlib>
//#include "svm_linker.h"

/*
int svm_linker::add_node_set(node* pnode, int num, double label)
{
	if (l + num >= 2 * max_set_idx) {
		std::cerr << "exceed maximium samples " << 2 * max_set_idx <<" in default.\n";
		exit(-1);
	}
	for (int i = 0; i < num; i++) {
		y[l] = label;
		x[l] = pnode[i].value;
		l++;
	}
	return l;
}

int svm_linker::keep_and_reset_rest(int idx)
{
	l = idx;
	return l;
}
*/
