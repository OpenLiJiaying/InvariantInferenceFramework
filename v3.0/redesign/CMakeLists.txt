cmake_minimum_required (VERSION 2.8)
set(CMAKE_C_COMPILER, "clang++")
set(CMAKE_CXX_COMPILER, "clang++")
project(iif)
include_directories(include)
aux_source_directory(src DIR_SRCS)
add_executable(iif ${DIR_SRCS})
